/*#include <stdio.h>

#include "common.h"
#include "bitmap.h"
#include "spdk_interface.h"

int
main()
{
    char *buf = (char *)malloc(BLOCK_SIZE);
	int  i, j;
	
	spdk_init();
	
	for (i = 0; i < 10; i++){
		clear_block(i);
	}

	for (i = 0; i < 10; i++) {
		//clear_block(i);
		buf[0] = '1';
		spdk_read_and_write(buf, i, 1, WRITE);
	}

	for (i = 0; i < 10; i++ ){
		spdk_read_and_write(buf, i, 1, READ);
		printf("read word:\n");
	    for (j = 0; j < BLOCK_SIZE; j++) {
	        //buf[i] = 0x00;
			printf("%02X ", buf[j]);
		}
		printf("\nend!\n------------------------------------\n");
	}

	spdk_cleanup();
	free(buf);
	
	return 0;
}
*/
